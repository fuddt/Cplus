---
title: データとは何か？
footer: コンピュータシステム基礎研修 第1回
theme: default
pptx_name: 01_what_is_data_and_memory
---

# データとは何か？

## ― ビット・バイトから始まるメモリの正体 ―

コンピュータシステム基礎研修 第1回

---

# 本日のアジェンダ

## 「データがメモリにどう入っているか」をゼロから解き明かす

* **① 最小単位：bit と byte**
  * なぜ1バイトは 0〜255 なのか？
* **② メモリの正体**
  * 「1バイトの箱が並んだ巨大なロッカー街」
* **③ リトルエンディアンの罠**
  * int 100 の2バイト目をいじると何が起きるか？
* **④ バッファとは何か**
  * 生メモリに「型」というレンズを当てる感覚
* **⑤ 現場の落とし穴**
  * アライメントとパディングの恐怖

---

# 1. そもそも「データ」とは何か？

## コンピュータの世界は超極論すると「0 と 1」だけ

どんな画像も、プログラムも、文字列も、
最終的には **0 または 1** の並びで表現されています。

```text
最小単位: 1 bit = [0] または [1]
```

* **1 bit** で表せる状態は **2通り**（0, 1）
* **2 bit** で表せる状態は **4通り**（00, 01, 10, 11）
* **3 bit** で表せる状態は **8通り**
* **ビットが1つ増えるたびに、表現できる数は「2倍」になる**

---

# 1バイトはなぜ「0〜255」なのか？

## 8ビットをひとまとめにしたのが「1バイト（byte）」

```text
1 byte = 8 bit
2^8 = 256 通りの状態を表せる
```

* コンピュータの数値は多くの場合 **0 から数え始める**
* 256通りの状態を 0 から並べると **0 〜 255** になる
* 最小値: `00000000`（10進数で 0）
* 最大値: `11111111`（128+64+32+16+8+4+2+1 = 255）

### 一行で言えば
**1バイトとは「0から255までの整数が1個入る小さな箱」である**

---

# C++の基本型で見てみる

## uint8_t と int は何が違うのか？

```text
【uint8_t】 1 byte（8 bit）
[ 0〜255 ]  ← 1バイトそのもの

【int】      4 byte（32 bit）※一般的な環境
[ byte 0 ][ byte 1 ][ byte 2 ][ byte 3 ]
約 -21億 〜 +21億 までの大きな数値を扱える
```

* **`uint8_t`**: 符号なし8ビット整数。マイナスを扱わず 0〜255
* **`int`**: 4つの箱（4バイト）を合体させて1つの大きな数値を表す
* 100 という数値なら、1バイト（0〜255）に十分収まる
* **では、int の 100 はメモリの中でどう入っているのか？**

---

# 2. メモリの正体とは？

## 「1バイトの箱がズラリと一列に並んだ巨大なロッカー街」

メモリ（RAM）の構造は、驚くほどシンプルです。

```text
アドレス:  0x1000  0x1001  0x1002  0x1003  0x1004 ...
メモリ:   [ 1byte ][ 1byte ][ 1byte ][ 1byte ][ 1byte ]
             ↑
           この箱1つ1つに 0〜255 の数値が入る
```

* メモリの最小単位は **常に 1バイト**
* 各箱には重複しない番号札 **「アドレス（番地）」** が付いている
* ポインタとは、単にこの **「ロッカーの番号札（番地）」** のこと
* どんな複雑な構造体も、メモリ上では **1列に並んでいるだけ**

---

# 3. リトルエンディアンの罠

## 人間の直感と、CPUの並べ順は「逆」である

`int x = 100;`（16進数で `0x00000064`）をメモリに置くとき:

```text
【人間の直感（ビッグエンディアン）】
上位桁から素直に置く
[ 00 ][ 00 ][ 00 ][ 64 ]

【実際のPC / x86・ARM（リトルエンディアン）】
下位桁（小さい桁）から先に置く！
[ 64 ][ 00 ][ 00 ][ 00 ]
  1B    2B    3B    4B
```

* x86 や ARM などの主要 CPU は **リトルエンディアン** を採用
* メモリの先頭（若い番地）に、一番下の桁が配置される

---

# 【実験】2バイト目を書き換えると何が起きるか？

## int 100 の 2バイト目を 00 → 01 に変更してみる

```text
変更前（100）:
[ 64 ][ 00 ][ 00 ][ 00 ]  → 0x00000064 = 10進数の 100

変更後（2バイト目を 01 に書き換え）:
[ 64 ][ 01 ][ 00 ][ 00 ]  → 0x00000164 = 10進数でいくつ？
```

* 直感だと「101」になりそう？
* **いいえ、101 にはなりません！**
* 2バイト目の `01` は、16進数で `0x0100`（10進数で **256**）の重みを持つ
* `256 + 100 = 356` に化ける！

---

# 実験コード（C++）

## 実際にメモリを直接いじって確かめる

```cpp
#include <iostream>
#include <cstdint>

int main() {
    int x = 100;
    uint8_t* p = reinterpret_cast<uint8_t*>(&x);

    std::cout << "変更前: " << x << std::endl; // 100

    p[1] = 1; // 2バイト目を直接書き換える！

    std::cout << "変更後: " << x << std::endl; // 356
    return 0;
}
```

* **ポインタを使って 2バイト目（p[1]）だけを書き換えた**
* 結果は見事に **356** になる
* これが「メモリの上で何が起きているか」を直接見るということ

---

# 4. バッファとは何か？

## 生メモリに「型」というレンズを当てる感覚

実務でよく聞く「バッファ」という言葉。
正体は **ただの連続したメモリ領域（バイト配列）** です。

```text
生メモリ:
[ 64 ][ 01 ][ 00 ][ 00 ][ 05 ][ 00 ][ 00 ][ 00 ] ...

レンズA（int* として見る）:
[      356      ][       5       ] → 2つの int に見える！

レンズB（uint8_t* として見る）:
[ 64 ][ 1 ][ 0 ][ 0 ][ 5 ][ 0 ][ 0 ][ 0 ] → 8個のバイトに見える！
```

* メモリそのものには「型」など存在しない
* プログラムが **「この4バイトを int として解釈する」** と決めているだけ

---

# 5. 現場の落とし穴① アライメント

## なぜCPUは「中途半端なアドレス」を嫌うのか？

CPUはメモリからデータを読むとき、1バイトずつチマチマ読みません。
**4バイトや8バイトのブロック単位** で一気に読み込みます。

```text
4バイト境界（0, 4, 8, 12番地...）に揃っている場合:
[ アドレス 0x0004 ] ---- 1回のアクセスで一撃読み込み！

境界をまたいでいる場合（0x0003 から 4バイト）:
[ ブロック 0 ][ ブロック 1 ] ---- 2回読んで結合する（遅い＆CPUが悲鳴）
```

* **境界アライメント（Alignment）**: データを型に応じた境界に配置するルール
* 4バイトの `int` は、4の倍数番地に置かなければならない
* これを守らないと、**処理速度の低下やクラッシュ** を招く

---

# 現場の落とし穴② パディング

## 構造体のサイズが見た目と合わない謎

```cpp
struct PlayerData {
    uint8_t  id;    // 1バイト
    int      hp;    // 4バイト
};
// 1 + 4 = 5バイト？
std::cout << sizeof(PlayerData); // → 出力は「8」になる！
```

```text
実際のメモリ配置:
[ id (1B) ][ パディング空き地 (3B) ][      hp (4B)      ]
0x00        0x01 〜 0x03             0x04 〜 0x07
```

* `hp` を4の倍数番地に置くため、コンパイラが自動で **3バイトの隙間** を詰める
* この隙間を **パディング（Padding）** と呼ぶ
* **「構造体のサイズ ＝ 各メンバの足し算」とは限らない！**

---

# これが実務でどう問題になるか？

## 通信やファイル保存で絶対にやってはいけないこと

パディングを考慮せずに、構造体をそのままファイルに保存すると:

```text
保存した側（パディングが 3バイト入った状態）
[ id ][ pad ][ pad ][ pad ][ hp ][ hp ][ hp ][ hp ] (8バイト)

別の環境・別の言語（パディングなしを期待）
[ id ][ hp ][ hp ][ hp ][ hp ] (5バイトのはず！)
  ↓
データが 3バイト分まるごとズレて大惨事！
```

* 通信パケットや独自バイナリファイルでは、**パディングの混入** が最大のバグ源
* だから低レイヤでは「1バイトの並び」を正確に制御する必要がある

---

# 本日のまとめ

## 持ち帰ってほしい 4つの重要ポイント

* **① メモリは1バイトの箱が一列に並んだ世界**
  * すべてのデータは、この箱の連続にすぎない
* **② リトルエンディアンを忘れない**
  * PCのメモリでは「小さい桁」から先に並ぶ
* **③ 型はメモリを見る「レンズ」**
  * 同じバイト列でも、int で見るか byte で見るかで意味が変わる
* **④ パディングを甘く見ない**
  * CPUの都合で勝手に隙間（余白）が空くことを常に意識する

---

# 次回予告

## 画像データの正体 —— 2次元の絵を1次元メモリにどう並べるか？

* 「画像も実はただの巨大なバイト配列だった」
* 1ピクセルは何バイト？ RGBはどう並ぶ？
* 現場で画像が斜めに崩れる原因: **LineSpan（ストライド）** を解剖する
